{
  "name": "@outfitter/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx serve -l 3030 public",
    "deploy": "bunx wrangler pages deploy public --project-name=outfitter-web"
  }
}