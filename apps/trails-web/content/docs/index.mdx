---
title: Trails
description: Agent-native, contract-first TypeScript. Define once, surface everywhere.
---

Welcome to the Trails documentation. This page is a placeholder shell — the home for Trails content that will be projected from the framework's own contracts once that seam is wired (see the web-platform architecture doc).

<FieldNote label="Dogfood">
  This callout is an `@outfitter/ui` Tier 1 component, rendered inside Fumadocs
  MDX through the app's `getMDXComponents`. Tier 0 tokens → Tier 1 component →
  Next app: the whole reuse chain in one paragraph.
</FieldNote>

## What is Trails?

Define a trail once with typed input, a `Result` output, examples, and meta — then surface it on CLI, MCP, HTTP, or a plain TypeScript library call. One contract, many surfaces, zero divergence.

## Next steps

Head to [Getting Started](/docs/getting-started) to stand up your first trail.
